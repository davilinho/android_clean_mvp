apply plugin: 'java'

dependencies {

    def lib = parent.ext

    compile lib.okhttp
    compile lib.retrofit

    compile project(':repository')

    testCompile lib.junit
    testCompile lib.mockito
}